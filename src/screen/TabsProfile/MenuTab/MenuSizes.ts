import SIZE from '../../../vars/SIZE'

export enum MENU_SIZE {
    SALE_SLIDER_HEIGHT = SIZE.width * 0.35,
    CAT_SLIDER_HEIGHT = 55,
    MENU_TITLE_HEIGHT = 50,
    MENU_ITEM_HEIGHT = SIZE.width * 0.5
}